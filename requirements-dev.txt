# Dev/test environment includes runtime dependencies because tests import
# production modules (PDF, prompts, parsers, Telegram/Gemini glue).
-r requirements.txt

pytest>=8.0.0,<9.0.0
pytest-asyncio>=0.23.0,<1.0.0
ruff>=0.5.0,<1.0.0
