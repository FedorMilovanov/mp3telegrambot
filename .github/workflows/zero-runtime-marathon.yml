name: Zero Runtime Marathon

on:
  push:
    branches:
      - agent/zero-runtime-surgery-marathon
    paths:
      - .zero-runtime-trigger

permissions:
  contents: read

jobs:
  repo-wide-audit:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: agent/zero-runtime-surgery-marathon
          fetch-depth: 0
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - name: Compile production tree
        run: python -m compileall -q services tools/voxcpm2 handlers core pipelines main.py bot_new.py
      - name: Runtime surgery AST audit
        run: python tools/runtime_surgery_audit.py
      - name: Reference map
        run: python tools/runtime_reference_audit.py
      - name: Shadow collision count
        run: python tools/voxcpm_collision_audit.py
