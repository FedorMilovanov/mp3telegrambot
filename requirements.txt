# Системные зависимости (не pip, установить отдельно):
#   ffmpeg  — для пересжатия аудио в 64kbps
#   nodejs>=22 или deno>=2.3 — для yt-dlp js-runtimes (обход YouTube n/SABR challenge)
#   Noto Sans — шрифт для субтитров Shorts (fonts.google.com/noto)

# ── Telegram Bot ──────────────────────────────────────────────────────────────
python-telegram-bot[socks]>=22.5,<23.0
# FIXED #40: обновлено с >=21.5,<22.0; PTB 22.5+ содержит критический фикс бесконечного retry в run_polling и поддержку Bot API 9.x

# ── YouTube downloader ────────────────────────────────────────────────────────
# FIXED v21 (#6): убрана верхняя граница <2026.0.0; добавлен экстра [default]
# (включает mutagen, pycryptodomex, websockets — нужны для части форматов)
# >=2026.7.4: фикс CVE-2026-55404 (command injection через --write-link);
# yt-dlp[default] тянет yt-dlp-ejs — обновлять их ВМЕСТЕ.
yt-dlp[default]>=2026.7.4

# ── HTTP-клиент ───────────────────────────────────────────────────────────────
# FIXED v21 (#5): зависимость была в коде (bot.py:24), но не задекларирована
requests>=2.31.0

# ── Обработка обложек ─────────────────────────────────────────────────────────
Pillow>=10.0.0,<12.0.0

# ── Gemini AI ─────────────────────────────────────────────────────────────────
# Опционально — бот работает и без него
# Floor 2.0: thinking_level появился в 1.51, minimal/medium — в 1.56;
# breaking changes 2.0 касаются только Interactions API, generate_content не тронут.
google-genai>=2.0.0,<3.0.0

# ── Health-check сервер ───────────────────────────────────────────────────────
flask>=3.0.0,<4.0.0

# ── Production WSGI-сервер ────────────────────────────────────────────────────
# Заменяет Flask dev-сервер в main()
waitress>=3.0.0,<4.0.0

# ── Переменные окружения ──────────────────────────────────────────────────────
python-dotenv>=1.0.0,<2.0.0

# ── Субтитры для Shorts ───────────────────────────────────────────────────────
# Опционально — бот работает и без него
# При первом запуске скачивает модель Whisper small (~460MB)
faster-whisper>=1.0.0

# FIXED (#25): numpy — lazy import в bot.py (строка 813), нужен для субтитров
numpy>=1.24.0

# ── PDF-генератор (pdf_generator.py) ─────────────────────────────────────────
# FIXED (#26): pdfkit и beautifulsoup4 использовались в коде, но не были
# задекларированы (pdf_generator.py строки ~217, ~224)
pdfkit>=1.0.0
beautifulsoup4>=4.12.0

# Часовой пояс Europe/Moscow для сброса дневных лимитов (AUDIT L8).
# На Windows нет системной IANA tz-базы — без tzdata ZoneInfo("Europe/Moscow")
# падает и лимиты тихо сбрасываются по локальному времени сервера.
tzdata>=2024.1
