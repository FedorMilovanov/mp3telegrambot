# Системные зависимости (не pip, установить отдельно):
#   ffmpeg  — для пересжатия аудио в 64kbps
#   nodejs  — для yt-dlp js-runtimes (ускоряет обход защиты YouTube)
#   Noto Sans — шрифт для субтитров Shorts (fonts.google.com/noto)

# Telegram Bot
python-telegram-bot>=21.5,<22.0

# YouTube downloader
yt-dlp>=2025.1.1,<2026.0.0

# Обработка обложек
Pillow>=10.0.0,<12.0.0

# Gemini AI (опционально — бот работает и без него)
google-genai>=1.5.0,<2.0.0

# Health-check сервер
flask>=3.0.0,<4.0.0

# Production WSGI-сервер (заменяет Flask dev-сервер в main())
waitress>=3.0.0,<4.0.0

# Переменные окружения
python-dotenv>=1.0.0,<2.0.0

# Субтитры для Shorts (опционально — бот работает и без него)
# При первом запуске скачивает модель Whisper small (~460MB)
faster-whisper>=1.0.0